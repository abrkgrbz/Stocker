# =====================================================
# Stocker API - Environment Variables for Coolify
# =====================================================
# Copy this file to .env and fill in your actual values
# These variables will be injected by Coolify at runtime
# =====================================================

# ==================================
# AZURE KEY VAULT (REQUIRED)
# ==================================
# All sensitive secrets are stored in Azure Key Vault
# Replace these with your actual Service Principal credentials
AZURE_KEY_VAULT_URI=https://stocker-kv-prod.vault.azure.net/
AZURE_TENANT_ID=3ab0756a-679c-49a0-8818-4712ab51c16e
AZURE_CLIENT_ID=a453fe7e-8edf-4a82-929d-81357b266c77
AZURE_CLIENT_SECRET=your-service-principal-secret-here

# ==================================
# DATABASE CONNECTIONS (REQUIRED)
# ==================================
# Database connections still need to be configured
# Note: DB passwords can also be moved to Key Vault
ConnectionStrings__MasterConnection=Server=sqlserver,1433;Database=Stocker_Master;User Id=sa;Password=${SA_PASSWORD};TrustServerCertificate=True
ConnectionStrings__TenantConnection=Server=sqlserver,1433;User Id=sa;Password=${SA_PASSWORD};TrustServerCertificate=True
ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=Stocker_Master;User Id=sa;Password=${SA_PASSWORD};TrustServerCertificate=True
ConnectionStrings__HangfireConnection=Server=sqlserver,1433;Database=Stocker_Hangfire;User Id=sa;Password=${SA_PASSWORD};TrustServerCertificate=True

# ==================================
# EXTERNAL SERVICES
# ==================================
# Only needed if not using Azure Key Vault for these
SEQ_API_KEY=YourSeqApiKey123
SMTP_USERNAME=your-email@gmail.com
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=MinioPassword123!
REDIS_PASSWORD=RedisPassword123!

# ==================================
# SECRETS MANAGED BY AZURE KEY VAULT
# ==================================
# The following secrets are automatically fetched from Azure Key Vault:
# - jwt-secret -> JwtSettings:Secret
# - smtp-password -> EmailSettings:SmtpPassword
# - rabbitmq-password -> RabbitMQ:Password
# - db-password -> Can be used for database connections
#
# No need to set these as environment variables anymore!
# They will be automatically loaded at application startup.
#
# To add new secrets to Key Vault:
# az keyvault secret set --vault-name "stocker-kv-prod" --name "secret-name" --value "secret-value"