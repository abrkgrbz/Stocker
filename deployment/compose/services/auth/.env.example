# =============================================================================
# Stocker Auth Service (Next.js) - Environment Variables Template
# =============================================================================

# CRITICAL SECRETS (REQUIRED - Must be at least 32 characters)
# Generate with: openssl rand -base64 48

# HMAC Secret (MUST BE SAME as web service)
HMAC_SECRET=ChangeThisToASecure32CharacterOrLongerSecretKeyForProductionUse123!

# JWT Secret (MUST BE SAME as web service)
JWT_SECRET=ChangeThisToASecure32CharacterOrLongerSecretKeyForProductionUse123!

# NextAuth Secret
NEXTAUTH_SECRET=YourNextAuthSecret_ChangeThis_MinLength32Characters!

# Session Secret
SESSION_SECRET=YourSecureSessionSecret32CharactersOrLongerForProductionChangeThis!

# =============================================================================
# NOTE: Other environment variables are configured in docker-compose.yml
# =============================================================================
# Build-time variables (passed as build args):
# - NEXT_PUBLIC_API_URL=https://api.stoocker.app
# - NEXT_PUBLIC_APP_URL=https://auth.stoocker.app
# - NEXT_PUBLIC_MAIN_APP_URL=https://stoocker.app
#
# Runtime variables (configured in docker-compose.yml):
# - NEXT_PUBLIC_AUTH_DOMAIN=https://auth.stoocker.app
# - NEXT_PUBLIC_BASE_DOMAIN=stoocker.app
# - API_INTERNAL_URL=http://api:5000
# - COOKIE_BASE_DOMAIN=stoocker.app
# - NODE_ENV=production
#
# Email configuration (optional):
# - SMTP_HOST=mail.privateemail.com
# - SMTP_PORT=465
# - SMTP_USER=info@stoocker.app
# - SMTP_PASSWORD=<your email password>
#
# =============================================================================
# IMPORTANT: Auth and Web services MUST share these secrets:
# - HMAC_SECRET (for cross-service token validation)
# - JWT_SECRET (for cross-service JWT validation)
# - SESSION_SECRET (for cross-subdomain sessions)
# =============================================================================
