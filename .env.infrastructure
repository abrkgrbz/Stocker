# ===================================
# STOCKER INFRASTRUCTURE ENVIRONMENT
# ===================================

# -----------------------------
# MinIO Object Storage
# -----------------------------
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=StockerMinio2024!
MINIO_APP_USER=stockerapp
MINIO_APP_PASSWORD=StockerApp2024!
MINIO_REGION=us-east-1

# -----------------------------
# RabbitMQ Message Queue
# -----------------------------
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=StockerRabbit2024!
RABBITMQ_APP_USER=stockerapp
RABBITMQ_APP_PASSWORD=StockerApp2024!
RABBITMQ_VHOST=stocker

# -----------------------------
# ElasticSearch & Kibana
# -----------------------------
ELASTIC_USERNAME=elastic
ELASTIC_PASSWORD=StockerElastic2024!

# -----------------------------
# Grafana Monitoring
# -----------------------------
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=StockerGrafana2024!
GRAFANA_SMTP_ENABLED=true

# -----------------------------
# Backup Solution
# -----------------------------
RESTIC_PASSWORD=StockerBackup2024!
RESTIC_USERNAME=admin
BACKUP_DB_USER=restic
BACKUP_DB_PASSWORD=ResticBackup2024!
BACKUP_SCHEDULE=0 2 * * *
DISABLE_AUTH=false

# -----------------------------
# Shared Email Settings (SendGrid)
# -----------------------------
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USERNAME=apikey
SENDGRID_API_KEY=YOUR_SENDGRID_API_KEY_HERE
FROM_EMAIL=info@stoocker.app
FROM_NAME=Stoocker
SMTP_ENABLE_SSL=true
SMTP_USE_STARTTLS=true

# -----------------------------
# Database (Reference from main)
# -----------------------------
SA_PASSWORD=YourStrongPassword123!
DB_HOST=database
DB_USER=sa
DB_PASSWORD=YourStrongPassword123!
DB_NAME=StockerTenantDb
MASTER_DB_NAME=StockerMasterDb
TENANT_DB_NAME=StockerTenantDb

# -----------------------------
# Redis (Reference from main)
# -----------------------------
REDIS_CONNECTION=redis:6379

# -----------------------------
# Seq Logging (Reference from main)
# -----------------------------
SEQ_URL=http://seq:5341
SEQ_API_KEY=
LOG_LEVEL=Information